# TUS CLASS Helper

東京理科大学 CLASS（`class.admin.tus.ac.jp`）向けの Chrome 拡張機能。

## 機能

1. **「+ 追加」ボタン** — シラバス拡大ダイアログに表示（一覧画面からは不完全な情報しか取れないため、一覧の行への追加ボタンは無し。追加は詳細画面からのみ）
2. **拡張機能独自ページ** — `popup` アイコンクリック → ダッシュボードが新規タブで開く
   - **シラバス比較タブ**: 保存した授業を2つ以上選択して項目別横並び比較（スコア表示・スコア降順ソート対応）
   - **時間割表タブ**: 月〜土 × 1〜8限のグリッドに自動配置（重複検出、手動メモ対応）。履修予定の**合計単位数**を表示。各コマに講座メモ・成績スコアも表示
- **講座メモ**: 各カードの「メモ」ボタンで自由文を保存。比較・時間割・成績タブと詳細モーダルに表示
- **講座の色付け**: 各カードの「色」ボタンで色を保存（パレット＋カスタム色・クリア可）。比較・時間割・成績タブにドット表示し、時間割のコマ・各カードの背景色も講座色で薄く染めて区別しやすくする。**色を指定していない科目は白背景（グレーの左ボーダーのみ）**で表示。編集は比較・成績タブから
- **右側サイドバー**（CLASS上）：縮小タブで常駐し、クリックで展開。ミニ時間割（履修予定）＋追加講座一覧（履修予定のみ切替可）。右上「«」で縮小、「×」で非表示（ポップアップから再表示）。状態は保存され複数タブで同期。パネルの幅・文字サイズは設定タブから変更可（保存すると即時反映）
   - **成績リンクタブ**: 保存済み授業を全件表示 → 各授業の「成績を接続」から成績照会の候補一覧を選んでリンク／解除
   - **設定タブ**: スコア重み設定、JSON エクスポート/インポート、全データ削除
3. **成績一覧の行ボタン** — 成績評価公開授業照会の各行に、保存済み授業があれば「成績をリンク」、無ければ「＋成績追加」（成績リンク用のみ保存。シラバス比較には追加しない）。成績ページで開いた詳細ダイアログには追加ボタンを出さない
4. **成績スコア（5〜0点）** — 成績分布から `S×5＋A×4＋B×3＋C×2＋D×0 ÷ 100` で算出。成績評価状況の横にスコア列を追加し、検索結果をスコア降順に並べ替え可能（表示中ページ内）
5. **バッジ表示** — ツールバーアイコンに保存件数を表示

## データ保存

`chrome.storage.local`（このPCのみ）。構造:

```
courses:     { [classCode]: Course }     // 授業本体（基本 + シラバス詳細 + 公開成績）
gradeLinks:  { [classCode]: { s,a,b,c,d, count, score, linkedAt, sourceRow } }
gradeRows:   { [classCode]: { classCode, nameJa, grades, score } }  // 成績照会の行候補（自動収集）
timetable:   { [classCode]: { planned } }
manualSlots: { "月-4": { title, note } }
settings:    { scoreWeights: { s,a,b,c,d }, sidebar: { enabled, expanded, plannedOnly, panelWidth, fontScale } }
```

## インストール

### 方法1: Releases から CRX をダウンロード（推奨）

1. GitHub の **Releases** ページ（[https://github.com/haya9924/tus_syllabus_assist/releases](https://github.com/haya9924/tus_syllabus_assist/releases)）を開く
2. 最新のリリース（例: `v0.9.6`）の **Assets** 欄から **`tus_syllabus_assist.crx`** をダウンロード
3. Chrome アドレスバーに `chrome://extensions/` を入力して開く
4. 右上の **「デベロッパーモード」** を **ON** にする
5. ダウンロードした **`.crx` ファイルを chrome://extensions/ のページ上へドラッグ＆ドロップ**
6. 確認ダイアログの **「拡張機能を追加」** をクリック
7. ツールバーにアイコンが表示されたら完了

> Chrome Web Store 経由ではないため、起動時に「この拡張機能を削除しますか？」の確認が出ることがあります。「キャンセル」で使い続けられます。

### 方法2: ソースから読み込む（開発者向け）

1. このリポジトリを `git clone` する（または ZIP ダウンロードして展開）
2. Chrome アドレスバーで `chrome://extensions/` を開く
3. 右上の「デベロッパーモード」を ON
4. 「パッケージ化されていない拡張機能を読み込む」 → リポジトリのフォルダ（`manifest.json` がある階層）を選択
5. ツールバーにアイコンが表示されたら完了

> プライバシー: `*.pem`（署名キー）と `*.crx`（パッケージ）は `.gitignore` で除外され、リポジトリにはアップロードされません。署名キーは拡張機能IDの生成に使うため**第三者に渡さないでください**。

## 使い方

### はじめての方へ：成績データの保存とリンク（全体の流れ）

「成績評価公開授業照会」のページ上に表示される **拡張機能のボタン** だけで成績データを保存できます。ボタンは科目の行ごとに、その科目の状態に合わせて自動で切り替わります。

**① 成績評価公開授業照会を開いて検索する**
- いつもの検索結果の表に、拡張機能のボタン（＋）が各行に追加されます

**② 行のボタンを見てクリックするだけ**
- まだ何も登録していない科目 → **「＋成績追加」** をクリック
  - その行の **成績分布（S/A/B/C/D）とスコア** を「成績リンク用」に保存します（シラバス比較には追加されません）
  - 保存後はボタンが **「成績保存済み」**（グレーで無効）に変わります
- シラバス照会で**すでに登録してある科目** → **「成績をリンク」** というボタンになっています
  - クリックすると **保存済み授業を選ぶ画面（候補一覧）** が開くので、その行の成績を紐付ける授業を選ぶと、その場でリンク完了
  - リンク済みの行は **「成績リンク済」** と表示され、クリックでリンク先の変更・編集もできます

**③ 保存した成績はダッシュボードの「成績リンク」タブに並ぶ**
- ツールバーの拡張機能アイコン →「ダッシュボードを開く」→ **成績リンクタブ**
- ここで全件を確認でき、**「成績を接続」** から保存済み授業にリンク／解除、数値の直接編集もできます
- 「＋成績追加」で保存しただけの未接続のものは、成績照会ページの下部の **「保存した成績データ（未接続）」** にも並びます

**④ シラバス照会（科目詳細）から登録すれば自動リンクもできる**
- 検索結果で科目名リンク → シラバス照会ダイアログ → **「+ 追加」** をクリック
- シラバスの全項目を保存しつつ、**未接続の保存成績と授業コードが一致する科目があれば自動でリンク**されて「追加済み✓」まで一発で完了します
- 一致する未接続の保存成績があるときは、ダイアログ内に **「成績をリンク」ボタン** も表示されます。押すと候補のモーダル（成績分布・スコア付き）が開き、候補を選ぶと詳細保存＋リンクを同時に行えます

> つまり、**「成績照会で＋成績追加 → シラバス照会で＋追加」** の順でも、**「シラバス照会で＋追加 → 成績照会で成績をリンク」** の順でも、どちらからでも最後に成績と授業を 1 つにまとめられます。順番はどちらが先でも構いません。

### シラバス詳細から追加（唯一の追加経路）
一覧画面の行からは完全なシラバスが取れないため、一覧の行に「+ 追加」ボタンは出しません。追加はシラバス詳細画面からのみ行います（ダイアログには追加ボタンと、一致時のみ現れるリンクボタンの最大2つを表示）。
1. 検索結果で科目名リンクをクリック → シラバス照会ダイアログが開く
2. 右上（ふせん/印刷ボタンの横）の **「+ 追加」** をクリック
   - 全項目（概要・目的・到達目標・成績評価方法・教科書・授業計画・備考・BYOD等）を保存
   - 未接続の保存成績（「＋成績追加」で保存した行）があれば**同時にリンク**し、ボタン一発で「追加済み✓」まで完了します
   - 別科目を開き直してもボタンが追従（AJAX再描画対応）
   - 旧データで成績分布側だけ追加済みの場合もボタンは **「+ 追加」** のまま有効化され、押下で詳細保存＋リンクが同時に完了します

### 片側だけ追加済みの場合の小さめ注意表示
授業コードで成績分布側とシラバス側の同一性を確認し、保存済みでも当該側が未完了の場合は行内にボタンより一回り小さいアンバー文字の注意書きを出します（追加ボタンは出しません）。「＋成績追加」の明示保存（授業なし）も成績側の登録とみなします。
- 成績照会の行 → 「シラバス側ですでに追加されています」（リンク完了で消去）
- シラバス照会の行 → 「成績分布側ですでに追加されています」（詳細追加で消去）。一覧の注意書きは文字表示のみです
- シラバス詳細ダイアログ内にも同文言を表示。未接続の保存成績と授業コードが一致する場合のみ **「成績をリンク」ボタン** も併置し、押すと成績データ候補モーダル（分布・スコア付き）が開き、候補選択でシラバス詳細の全項目保存と同時にその候補をリンクします
- 一覧の描画タイミング取りこぼしに備え、定期再スキャン＋別タブの変更検知でも再表示します

### データの主従関係
- **基本はシラバス照会のデータ**：比較タブ・時間割タブ・サイドバーにはシラバス詳細まで取り込んだ講座のみ表示
- **成績側はあくまで補助**：成績分布・スコアのみ。成績のみのデータは成績リンクタブでのみ表示（「詳細未取得」バッジ付き）

### 成績分布の手動リンク（ダッシュボード起点）
1. ダッシュボードの **成績リンクタブ** に保存済み授業が全件表示される
2. 各授業の **「成績を接続」** → 成績照会の候補一覧（スコア降順・絞り込み可）から **「この成績にリンク」**
   - 候補は成績評価公開授業照会で検索を実行すると自動収集される
   - 接続済みは **「接続解除」**／接続先変更／数値の直接編集が可能
3. 成績照会の各行の **「成績をリンク」** ボタンから保存済み授業を選ぶ従来フローも利用可
4. 成績一覧で **「＋成績追加」** を押した行は、下の「保存した成績データ（未接続）」に並ぶ。ここから保存済み授業への紐付け・削除ができる（シラバス比較には追加されない）

### 成績スコアと並べ替え
- スコア = (S×5＋A×4＋B×3＋C×2＋D×0) ÷ 100（最大5.0〜最小0.0、重みは設定タブで変更可）
- 成績照会の表に **スコア列** を成績評価状況の横（D評価の右）に追加。**スコア列ヘッダーのクリック**または表上の「スコア降順に並べ替え」で表示中ページ内を高い順にソート（ヘッダー再クリック／「元の順序に戻す」で復元）
- ダッシュボードの比較タブでもスコア表示・スコア降順ソート、比較表に「成績スコア」行を追加

### ダッシュボード
- ツールバーアイコン → ポップアップ → 「ダッシュボードを開く」
- **シラバス比較**: カードのチェックで複数選択 → 「選択した授業を比較」→ 項目別横並び表示（相違は黄色ハイライト）+ 同時限の警告。**成績分布は単一グラフ（S/A/B/C/Dの各判定に講座ごとの系列バーを並べた形式）で視覚比較**できる（凡例付き・数値も併記）
- **時間割表**: カードの「履修予定に追加」で曜日時限に従い自動配置。重複コマは赤色。空きセルダブルクリックでメモ追加。追加リストの候補にも成績スコアを表示。**追加候補カードからもメモ・色を編集可**（保存するとコマ表示にも反映）
- **成績リンク**: 保存済み全件表示 → 接続／接続解除／直接編集
- **設定**: スコア重みの変更、サイドバー表示サイズの変更、JSON エクスポート/インポート、全データ削除

## ファイル構成

```
manifest.json          # MV3 マニフェスト
background.js          # バッジ更新 / ダッシュボードオープン
content/
  content.js           # ボタン注入 + MutationObserver
  content.css          # ボタン・ピッカーのスタイル
  parser.js            # シラバス / 検索行 / 曜日時限パーサ + ストレージラッパー
dashboard/
  dashboard.html       # 4タブのダッシュボード
  dashboard.js         # 比較 / 時間割 / 成績リンク / 設定のロジック
  dashboard.css
popup/
  popup.html           # 件数表示 + ダッシュボード起動
  popup.js
icons/
  icon{48,96,128}.png
  make_icon.py         # アイコン再生成用
test_parser.js         # パーサーユニットテスト（node + jsdom）
test_content_score.js  # スコア列の動作テスト（node + jsdom）
test_list_policy.js    # 一覧は追加不可・詳細のみ追加の動作テスト（node + jsdom）
test_dashboard_memo.js # メモ・スコア表示の動作テスト（node + jsdom）
test_grade_unlinked.js # 未接続成績セクションの動作テスト（node + jsdom）
test_sidebar.js        # サイドバーの動作テスト（node + jsdom）
test_sidebar_size.js   # サイドバーサイズ設定の動作テスト（node + jsdom）
test_course_color.js   # 講座色付けの動作テスト（node + jsdom）
test_tt_addlist_hist.js# 時間割追加候補のメモ・色編集＋比較ヒストグラムの動作テスト（node + jsdom）
```

## 開発・テスト

```bash
cd tus_syllabus_assist
npm install          # テストに必要な jsdom を導入
node test_parser.js         # パーサーのユニットテスト
node test_content_score.js  # スコア列の動作テスト
node test_list_policy.js    # 一覧追加不可ポリシーのテスト
node test_dashboard_memo.js # メモ・スコア表示のテスト
node test_grade_unlinked.js # 未接続成績セクションのテスト
node test_course_color.js   # 講座色付けのテスト
node test_sidebar.js        # サイドバーのテスト
node test_sidebar_size.js   # サイドバーサイズ設定のテスト
node test_tt_addlist_hist.js# 時間割追加候補＋比較ヒストグラムのテスト
```

## リリース方法（メンテナ向け）

```bash
# 署名キーを使ってパッケージ化（拡張機能IDを維持）
chrome --pack-extension=/path/to/tus_syllabus_assist \
       --pack-extension-key=/path/to/tus_syllabus_assist.pem
# 生成された tus_syllabus_assist.crx を GitHub Release の Assets に添付して公開
```

## 対応範囲

- 検索結果ページ: `https://class.admin.tus.ac.jp/uprx/up/bs/bsa001/Bsa00101.xhtml`、`https://class.admin.tus.ac.jp/uprx/up/xu/xut107/Xut10701.xhtml`、シラバス照会 `https://class.admin.tus.ac.jp/uprx/up/km/kmh006/Kmh00601.xhtml`（見出し判定のため他の検索画面でも動作する設計）
- シラバスダイアログ: `#pkx02301:dialog`（科目名リンクで開く）
- 対象曜日: 月〜土 / 1〜8限
- 集中講義など（曜日時限に数字が無いもの）は時間割に自動配置されず「履修予定に追加」リストに表示

## 注意事項

- 公式の学務システムへの変更でセレクタが壊れる可能性あり
- 成績データの二次配布は不可。本拡張は個人の履修検討用途
- 認証セッションが切れるとCLASS側の操作は不可だが、拡張に保存したデータは残る
